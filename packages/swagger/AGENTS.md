# Agent instructions — @prefabs.tech/fastify-swagger

This file is published **with the npm package**. Use it when the repo root is not available (for example under `node_modules`).

## Read order

1. [docs/llm/INDEX.md](./docs/llm/INDEX.md) — orientation for this package.
2. Workspace reference (deep links to GUIDE, `src/index.ts`, `plugin.ts`, tests — **not** inside this tarball):  
   https://github.com/prefabs-tech/fastify/blob/main/docs/llm/REFERENCE.md#swagger
3. Workspace map (all packages):  
   https://github.com/prefabs-tech/fastify/blob/main/docs/llm/INDEX.md

Open sources through links on that REFERENCE page on GitHub. Avoid scanning entire monorepo trees unless those docs are stale or insufficient.

## In this install

You typically have `dist/`, `docs/llm/`, this file, and `llms.txt`. For full source and tests, use the repository URL from `package.json` → `repository`.
